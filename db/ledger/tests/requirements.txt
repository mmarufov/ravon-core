# Python 3.11+. The harness is deliberately not Swift and not Kotlin: the
# invariant under test is a property of the *database*, so the test must be able
# to reach the database without going through any service that might be the
# thing at fault.
psycopg[binary]>=3.1,<4
pytest>=8.0
hypothesis>=6.100
